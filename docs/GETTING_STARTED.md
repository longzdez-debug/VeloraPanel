# Getting started

Install Python 3.12+, then run `run.bat` or `run.ps1`. Open http://127.0.0.1:8765. Copy `config/gamestate_integration_velora.cfg` to the CS2 cfg folder for live GSI. The default input backend is safe/no-op.
